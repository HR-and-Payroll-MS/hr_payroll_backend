import React from 'react';

const Icon = ({ name, className }) => {
    // Placeholder for icons. You can replace with actual SVG or icon library.
    return (
        <span className={className}>
            {name}
        </span>
    );
};

export default Icon;
